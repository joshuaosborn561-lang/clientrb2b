## Repo overview

This repo contains:

- `index.js` (repo root): **legacy worker**. Do not modify if you have an existing client running.
- `ui/`: **Railway UI** to add/manage clients + generate per-client env blocks.
- `worker-v2/`: **new worker** (LeadMagic → SmartLead email + HeyReach LinkedIn) for onboarding new clients.

### Deployment model (Railway)

- Keep your **legacy** Railway service pointed at the repo root as-is for the current client.
- For each **new client**, deploy a **new Railway service** from `worker-v2/` and paste the env block generated by the UI.

### Railway setup (recommended)

You will end up with **two kinds of Railway services**:

- **UI service** (one total)
  - Root Directory: `ui/`
  - Start command: `npm start`
  - Add a **Postgres** plugin (provides `DATABASE_URL`)
  - Required env vars: `UI_ADMIN_PASS` (optionally `UI_ADMIN_USER`, default `admin`)
  - Set `PGSSLMODE=require`

- **Worker v2 service** (one per client)
  - Root Directory: `worker-v2/`
  - Start command: `node index.js`
  - Add cron schedule (example): `*/15 * * * *`
  - Env vars: paste the block from the UI (plus real secrets)

Do **not** modify your legacy worker service while onboarding new clients.

